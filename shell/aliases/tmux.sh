# Tmux
alias tm='tmux'
alias tmkill='tmux kill-session'
# Tmuxinator
alias tmor='tmuxinator'

# Tmuxinator NOTE replaced by tmuxinator autocompletion script, which uses `mux`
# alias tmtor='tmuxinator'
